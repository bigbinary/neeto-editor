import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../lib";

<Meta
  title="Examples/Character count"
  parameters={{
    layout: "padded",
    viewMode: "docs",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Charater Count

neetoEditor can be configured to show the character count. This can be enabled
by passing a truthy value to the `isCharacterCountActive` prop.

<Canvas>
  <Story name="Character count">
    <Editor isCharacterCountActive />
  </Story>
</Canvas>
