import { Meta, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../src";

<Meta
  title="Examples/Editor height"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Editor height

neetoEditor can be configured to provided a custom initial height using the
`rows` prop. It accepts an integer value which represents the number of rows. It
uses the `strategy` prop to control whether the height is `fixed` or `flexible`.

<Canvas>
  <Editor rows={3} heightStrategy="flexible" />
</Canvas>
