import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../lib";

<Meta
  title="Examples/Placeholder"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Placeholder

A placeholder can be provided to the editor by passing string value to the
`placeholder` prop.

<Canvas>
  <Story name="Placeholder">
    <Editor placeholder="Input text here" />
  </Story>
</Canvas>
