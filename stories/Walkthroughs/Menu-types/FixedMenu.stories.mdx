import { Meta, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../../src";

<Meta
  title="Walkthroughs/MenuTypes/Fixed Menu"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Fixed Menu

The default neetoEditor layout comes with a set of always-on-top fixed menu
controls to interact with the editor.

<Canvas>
  <Editor />
</Canvas>
