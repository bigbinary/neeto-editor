import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../../src";

import Bubble from "../../assets/slash-command.mp4";

<Meta
  title="Walkthroughs/MenuTypes/Slash Command"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Slash Commands

Slash commands are actions that can be applied to block of text. This menu is
disabled by default. Setting the prop `hideSlashCommands` to `false` will enable
this menu and appears when user types `/` at start of new line.

<video src={Bubble} playsinline autoPlay muted loop />

#### **Example**

<Canvas>
  <Editor rows={40} menuType="bubble" heightStrategy="flexible" hideSlashCommands={false} />
</Canvas>
