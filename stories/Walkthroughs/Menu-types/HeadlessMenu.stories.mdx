import { Meta, Canvas } from "@storybook/addon-docs";
import { Editor } from "../../../src";

<Meta
  title="Walkthroughs/MenuTypes/Headless Menu"
  parameters={{
    layout: "padded",
    previewTabs: {
      canvas: {
        hidden: true,
      },
    },
  }}
  component={Editor}
/>

# Headless Menu

The `headless` menu component will let you customize the menu as you want. It is
the editor menu in it's most raw format.

<Canvas>
  <Editor
    autoFocus
    menuType="headless"
    addons={[
      "highlight",
      "emoji",
      "code-block",
      "block-quote",
      "image-upload",
      "video-upload",
    ]}
  />
</Canvas>
